namespace Rinsen.Heru
{
    public enum Setting
    {
        UnitOn,
        OverpressureMode,
        BoostMode,
        AwayMode,
        ClearAlarms,
        ResetFilterTimer,
        ExtendOperation,
    }
}
